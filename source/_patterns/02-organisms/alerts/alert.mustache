<div class="alert__{{alert.class}}">
	<h2 class="alert--heading"><span class="sbuicon-info2"></span>{{alert.title}}</h2>
	<div class="alert--summary">
	{{excerpt.short}}
	</div>

	 {{# cta }}
        <div class="alert--cta">
            <a href="{{cta.url}}" class="{{cta.class}}">{{cta.text}}</a>
        </div>
    {{/ cta }}
	
	</div>
	</div>
